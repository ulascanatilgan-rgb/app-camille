# Camille — French Agent for Ulas Atilgan

Upload-ready PWA build.

Changes in this version:
- Realtime model: `gpt-realtime-2.1-mini`
- All app UI labels are in English
- Header: `Camille - French Agent for Ulas Atilgan`
- PWA cache bumped to v2 so phones fetch the new interface

Railway environment variable required:
`OPENAI_API_KEY=...`
